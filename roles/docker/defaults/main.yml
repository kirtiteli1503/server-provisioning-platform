---
docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin

# Users added to the docker group (passwordless docker access). ansible_user is added by default below.
docker_extra_users: []
